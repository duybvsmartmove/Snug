{
  "name": "snug",
  "version": "0.1.0",
  "private": true,
  "description": "Everything Fits (Snug mobile) — game + Level Editor trong một repo",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "matter-js": "^0.20.0",
    "poly-decomp": "^0.3.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
