{
  "name": "snug",
  "version": "0.1.0",
  "private": true,
  "description": "Everything Fits (Snug mobile) — game + Level Editor trong một repo",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "build:www": "vite build && node tools/build-www.mjs",
    "android:sync": "npm run build:www && cap sync android",
    "android:open": "npm run android:sync && cap open android",
    "android:apk": "npm run android:sync && cd android && ./gradlew assembleDebug",
    "android:release": "npm run android:sync && cd android && ./gradlew assembleRelease",
    "android:install": "npm run android:apk && adb install -r android/app/build/outputs/apk/debug/*-debug.apk"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.2",
    "matter-js": "^0.20.0",
    "poly-decomp": "^0.3.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2",
    "vite": "^5.4.0"
  }
}
